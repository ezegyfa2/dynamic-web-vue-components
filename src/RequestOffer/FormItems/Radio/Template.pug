section
    h3.title {{ title }}
    div.form-check(v-for='option in options')
        input.form-check-input.extra-margin-input(
            type='radio' 
            ref='inputElement'
            v-bind:name='name'
            v-model='currentValue' 
            v-bind:value='option.value'
            v-bind:required='required'
        ) 
        | {{ option.label }}
