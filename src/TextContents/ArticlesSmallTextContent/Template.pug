.text-content.text-center
    .title(v-if='title') {{ title }}
    .content(v-if='content') {{ content }}
    .link
        a.btn.btn-primary.btn-l(v-if='url' role='button' href='url') {{ button_content }}