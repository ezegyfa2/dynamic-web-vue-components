section.page-section(v-bind:id='id')
    .container
        dynamic-component(
            v-bind:component_infos='title_section'
        )
        dynamic-component(
            v-bind:component_infos='thank_you_section'
        )
        p {{ thank_you_content }}
        p {{ email }} 
        a(:href="`mailto:${email_address}`") {{ email_address }}
        
